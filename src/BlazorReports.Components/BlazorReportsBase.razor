@code {
  /// <summary>
  /// Dictionary of global assets with their base64 encoded content as the value.
  /// </summary>
  [Parameter]
  public Dictionary<string, string> GlobalAssets { get; set; } = new();
}
